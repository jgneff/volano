#!/bin/bash
# netserver.sh - VOLANO Chat Server Benchmark network scalability test.
# Run this script before starting "netclient.sh" on another machine.
# Usage: netserver.sh <vendor>
./startup.sh server net $1

